# DeSi Configuration File
# This is a template. Copy this file to .env and modify the values as needed.

# --- Database Configuration ---
# Path to the ChromaDB vector store
DESI_DB_PATH=desi_vectordb
# Name of the collection inside ChromaDB
DESI_COLLECTION_NAME=desi_docs
# Path to the SQLite database for conversation history
DESI_MEMORY_DB_PATH=data/conversation_memory.db

# --- Model Configuration ---
# The Ollama model to use for generating answers and rewriting queries
DESI_MODEL_NAME=qwen3
# The Ollama model used for generating embeddings
DESI_EMBEDDING_MODEL_NAME=nomic-embed-text

# --- Data & Scraper Configuration ---
# Base directory for raw and processed data
DESI_DATA_DIR=data
DESI_PROCESSED_DATA_DIR=data/processed
# URLs for the scrapers
DESI_OPENBIS_URL=https://openbis.readthedocs.io/en/20.10.0-11/index.html
DESI_WIKIJS_URL=https://datastore.bam.de/en/home

# --- Query Configuration ---
# Number of relevant chunks to retrieve for a query
DESI_RETRIEVAL_TOP_K=5
# Number of past conversation turns to keep in memory for context
DESI_HISTORY_LIMIT=20

# --- Logging Configuration ---
# Log level (e.g., DEBUG, INFO, WARNING, ERROR)
DESI_LOG_LEVEL=INFO